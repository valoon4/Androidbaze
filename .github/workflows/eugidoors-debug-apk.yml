name: Build EugiDoors Debug APK

on:
  push:
    branches: [ "main" ]
    paths:
      - "games/EugiDoors/**"
      - ".github/workflows/eugidoors-debug-apk.yml"
  workflow_dispatch:

permissions:
  contents: read

jobs:
  build:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: games/EugiDoors
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'
      - uses: android-actions/setup-android@v3
      - name: Install Android 16 SDK
        run: |
          yes | sdkmanager --licenses >/dev/null || true
          sdkmanager "platform-tools" "platforms;android-36" "build-tools;36.0.0"
      - name: Install Gradle 8.11.1
        run: |
          curl -fsSL https://services.gradle.org/distributions/gradle-8.11.1-bin.zip -o /tmp/gradle.zip
          sudo mkdir -p /opt/gradle
          sudo unzip -q /tmp/gradle.zip -d /opt/gradle
          echo /opt/gradle/gradle-8.11.1/bin >> "$GITHUB_PATH"
      - name: Build EugiDoors debug APK
        run: gradle clean assembleDebug --no-daemon --stacktrace
      - name: Upload EugiDoors APK
        uses: actions/upload-artifact@v4
        with:
          name: EugiDoors-retro-debug
          path: games/EugiDoors/app/build/outputs/apk/debug/app-debug.apk
          if-no-files-found: error
          retention-days: 30
