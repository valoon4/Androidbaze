package com.eugi.apeeconomics

import android.content.Context

/**
 * v0.11 development entry point.
 * Keeps the proven realtime v0.9 simulation intact while v0.11 visual work
 * is iterated from device screenshots. This makes the version boundary real
 * in game code instead of merely naming the CI artifact v0.11.
 */
class PixelVillageV11(context: Context) : PixelVillageV09(context)
